問題1

次のコードを見てください。

int temperature = 25;
double value = temperature;

以下を自分の言葉で説明してください。

temperature のデータ型は何か
value のデータ型は何か
double value = temperature; では何が起きているか
この型変換を「暗黙的型変換」と呼ぶのはなぜか


解答：
temperature のデータ型はint型、整数型
value のデータ型はdouble型、浮動小数点型
double value = temperature; ではint型の25だったのが、double型の変数valueに入って25.0という小数点型になっている
この型変換を「暗黙的型変換」と呼ぶのはc言語が自動的に型変換をしているから


=======================================================================


問題2

次の2つのコードの違いを説明してください。

A
int a = 7;
int b = 2;

double result = a / b;


B
int a = 7;
int b = 2;

double result = (double)a / b;

以下を答えてください。

Aのresultには最終的に何が入るか
Bのresultには最終的に何が入るか
(double)aは何をしているか
なぜAでは小数部分が失われるのか


解答：
Aのresultには最終的に3が入る
Bのresultには最終的に3.5が入る
(double)a/bはintという整数のデータ型を少数のdoubleというのに明示的変換といって自ら書いて変換する方法で、(double)a/bでキャストという
なぜAでは小数部分が失われるのかについては、明示的変換であるキャストをしていないから

--------------------------------

解答：
Aのresultには最終的に3が入る
Bのresultには最終的に3.5が入る
(double)a/bはintという整数のデータ型を少数のdoubleというのに明示的変換といって自ら書いて変換する方法で、(double)a/bでキャストという
なぜAでは小数部分が失われるのかについては、/*割り算をする時点でaとbが双方intで整数のデータ型だから、先に 3 になり、そのあと double に代入されて 3.0 になる。答えの小数部分が出なくなる。*/


=======================================================================


問題3

次のコードを見てください。

short a = 100;
short b = 200;

int result = a + b;

以下を説明してください。

a の型は何か
b の型は何か
a + b を計算するとき、a と b は通常どの型へ変換されるか
この自動変換を何と呼ぶか


解答：
a の型はshort
b の型はshort
a + b を計算するとき、a と b は通常intの型へ自動変換される
この自動変換を暗黙的変換という

ちなみに整数型で1文字専用に表すときに使われるcharというデータ型も計算時になると暗黙的変換で数字に変換される

----------------------------

解答：
a の型はshort
b の型はshort
a + b を計算するとき、a と b は通常intの型へ自動変換される
この自動変換を暗黙的変換という

ちなみに/*charというデータ型もあるが、1文字を表す時に使われるがデータの型は整数型で内部では文字も数字に変換されている。計算の時に使われたとしても暗黙的変換の整数昇格で自動的にデータ型charがintに変換され、見た目というか外部上で文字も数字に変換される*/


=======================================================================


問題4

次のコードを見てください。


uint8_t value = 254;

value = value + 2;


以下を説明してください。

uint8_t が表現できる範囲
254 + 2 の本来の計算結果
最終的に value に入る値
なぜその値になるのか
この現象を何と呼ぶか


解答：
uint8_t が表現できる範囲は2の8乗で256通りが選択肢、組み合わせの数
254 + 2 の本来の計算結果は256
最終的に value に入る値は0である。
なぜその値になるのかは256通りだが0も数えての256通りだから0～255までしか入らない結果になる。超えると入らないから0という結果になる。
この現象を何と呼ぶかオーバーフローという

uint8_t が表現できる範囲は 0〜255。
2^8 = 256 は、表現できる値の「個数」。


=======================================================================


問題5

次のコードを見てください。


uint8_t value = 1;

value = value - 2;


以下を説明してください。

uint8_t が表現できる範囲
1 - 2 の本来の計算結果
最終的に value に入る値
なぜその値になるのか
この現象を何と呼ぶか


解答：
uint8_t が表現できる範囲は8bitだから2の8乗で組み合わせ、選択肢が256通りになる。
1 - 2 の本来の計算結果は-1
最終的に value に入る値は255
なぜその値になるのか、uint8_tのuはunsignedで0以上の数でしか現すことが出来ない。そして下限の0を超えると下限からbitの組み合わせ、選択肢の上限の256通り分を数えた255になる
この現象をアンダーフローという


=======================================================================


説明テスト

以下を、自分の言葉で説明してください。

暗黙的型変換とは何か
明示的型変換とは何か
キャストとは何か
次のコードで、なぜ 3.5 になるのか
int a = 7;
int b = 2;

double result = (double)a / b;
整数の昇格とは何か
char は文字を扱う型なのに、なぜ計算にも使えるのか
オーバーフローとは何か
アンダーフローとは何か
uint8_t で 255 + 1 → 0 になる理由
uint8_t で 0 - 1 → 255 になる理由

この10問をまとめて回答してください。



解答：

暗黙的型変換とはc言語が自動的にデータ型が違う時に型変換をしてくれること

明示的型変換とはc言語に任せる暗黙的変換をせずに自らがデータ型を変換する事

キャストとは(double)aである。

以下のコードで、なぜ 3.5 になるのかは7と2が双方int型で、そのまま割ると3という整数になるが、(double)a / b;の用に明示的変換のキャストをしている事によって、浮動小数点型のデータ型doubleにして計算を行なうことになり、結果3.5になる

int a = 7;
int b = 2;

double result = (double)a / b;


整数の昇格とは、charやshortなどの整数型でintより小さい整数のデータ型で計算をすると自動的にintのデータ型になるという事象

char は文字を扱う型なのに、なぜ計算にも使えるのかについてはcharは主に1文字を扱うが、その1文字も内部的に数字が割り当てられており、計算をするときは、その割り当てられた数字をもとに計算をするので、計算にも使える形になる。

オーバーフローとは、その変数のデータ型に割り当てられたbit数の扱える選択肢、組み合わせの数の上限と下限が決まっている中、その上限をはみ出して、その上限も含めた選択肢、組み合わせの数分の下限に戻る事をオーバーフローという

アンダーフローとは、その変数のデータ型に割り当てられたbit数の扱える選択肢、組み合わせの数で上限と下限が決まっている中であったり、unsignedで下限が決まっている中で下限を超えると、その下限も含めた選択肢、組み合わせの数分の上限に戻る事をアンダーフローという

uint8_t で 255 + 1 → 0 になる理由は8bitの扱える選択肢、組み合わせが256通りである。で、今回は0～255で256通りであるが、256という数字になると上限を超えてしまうため、下限の0になる。

uint8_t で 0 - 1 → 255 になる理由は8bitの扱える選択肢、組み合わせが256通りである。またuint8_tという事で、unsignedで0以上の数しか表せない。で、今回、下限の0を下回って―1となるため、0～255の256通りの上限である255になる。

------------------------------


解答：

暗黙的型変換とはc言語が自動的にデータ型が違う時に型変換をしてくれること

明示的型変換とはc言語に任せる暗黙的変換をせずに自らがデータ型を変換する事

キャストとは/*(変換したい型) を付けて、明示的に型変換する書き方・操作である。
例：(double)a*/

以下のコードで、なぜ 3.5 になるのかは7と2が双方int型で、そのまま割ると3という整数になるが、(double)a / b;の用に明示的変換のキャストをしている事によって、浮動小数点型のデータ型doubleにして計算を行なうことになり、結果3.5になる

int a = 7;
int b = 2;

double result = (double)a / b;


整数の昇格とは、charやshortなどの整数型でintより小さい整数のデータ型で計算をすると自動的にintのデータ型になるという事象

char は文字を扱う型なのに、なぜ計算にも使えるのかについてはcharは主に1文字を扱うが、その1文字も内部的に数字が割り当てられており、計算をするときは、その割り当てられた数字をもとに計算をするので、計算にも使える形になる。

オーバーフローとは、その変数のデータ型に割り当てられたbit数の扱える選択肢、組み合わせの数の上限と下限が決まっている中、その上限をはみ出して、その上限も含めた選択肢、組み合わせの数分の下限に戻る事をオーバーフローという

アンダーフローとは、その変数のデータ型に割り当てられたbit数の扱える選択肢、組み合わせの数で上限と下限が決まっている中であったり、unsignedで下限が決まっている中で下限を超えると、その下限も含めた選択肢、組み合わせの数分の上限に戻る事をアンダーフローという

uint8_t で 255 + 1 → 0 になる理由は8bitの扱える選択肢、組み合わせが256通りである。で、今回は0～255で256通りであるが、256という数字になると上限を超えてしまうため、下限の0になる。/*符号なし整数型に限り巡回する形みたいになる*/

uint8_t で 0 - 1 → 255 になる理由は8bitの扱える選択肢、組み合わせが256通りである。またuint8_tという事で、unsignedで0以上の数しか表せない。で、今回、下限の0を下回って―1となるため、0～255の256通りの上限である255になる。/*符号あり = -〇のような整数型じゃ巡回する形になるとは毎回限らない*/


=======================================================================


最終問題

次のコードを読んで、①〜⑧をすべて自分の言葉で説明してください。

#include <stdio.h>
#include <stdint.h>

int main(void)
{
    int a = 9;
    int b = 2;

    double result1 = a / b;
    double result2 = (double)a / b;

    short x = 100;
    short y = 50;
    int result3 = x + y;

    uint8_t max_value = 255;
    max_value = max_value + 1;

    uint8_t min_value = 0;
    min_value = min_value - 1;

    return 0;
}

① result1 に最終的に入る値と、その理由

② result2 に最終的に入る値と、その理由

③ (double)a のような書き方を何と呼び、何をしているのか

④ result1 の代入時に起きている暗黙的型変換について説明

⑤ x + y の計算時に起きていることと、その名称

⑥ max_value の最終的な値と、その理由。この現象の名称

⑦ min_value の最終的な値と、その理由。この現象の名称

⑧ 最後に、暗黙的型変換・明示的型変換（キャスト）・整数の昇格・オーバーフロー・アンダーフローの違いをまとめて説明してください。\



解答：
① result1 に最終的に入る値は4。理由はaとb双方がintという整数のデータ型であるから、整数で割って整数の結果しか出てこない。後からdoubleなどの浮動小数点型にしても整数から小数にならない。

② result2 に最終的に入る値は4.5。その理由はaとbをintの整数型から(double)a/bという明示的変換のキャストというものを行っているよう。計算をするときにdoubleの浮動小数点型にしているから計算結果も浮動小数点型の結果が出るから。

③ (double)a のような書き方をキャストと呼ぶ。元ある変数のデータ型から指定のデータ（今回はdouble）の型に変数のデータ型を明示的に変換しようとしている。

④ result1 の代入時に起きている暗黙的型変換とはc言語が自動的に元のデータ型の型から自動的に合う型変換を行う事である。

⑤ x + y の計算時に起きていることはshortという整数のデータ型からintという整数のデータ型に暗黙的変換がされている。その名称を整数の昇格という。shortより自動的にintに計算をすると自動的に変換される。

⑥ max_value の最終的な値は0。その理由として、変数に割り当てられたbitで扱える選択肢、組み合わせの数分、上限と下限の中に今回は数字を範囲として当てはめていくわけだが。選択肢、組み合わせの数分の上限を超えた範囲の数字を当てはめた結果、下限として設定されている値に変換される。この現象の名称をオーバーフローという。しかし、この巡回的な事は符号なし整数でしか起こらない。

⑦ min_value の最終的な値は255。その理由として変数に割り当てられたbitで扱える選択肢、組み合わせの数分、上限と下限の中に今回は数字を範囲として当てはめていくわけだが。選択肢、組み合わせの数分の下限を超えた範囲の数字を当てはめた結果、上限として設定されている値に変換される。この現象の名称をアンダーフローという。今回は巡回的に下限を超えたから上限に設定されている値になったが、符号ありの整数である今回の場合も巡回の用に値が出るわけではない。

⑧ 最後に、暗黙的型変換・明示的型変換（キャスト）・整数の昇格・オーバーフロー・アンダーフローの違いをまとめて説明してください。
暗黙型変換と明示的変換の違いはc言語が自動的に変換してくれる前者に対して、後者は人間が明示的に変換を指示、キャストという型変換に必要なものを書かないといけない。
整数の昇格はshortやcharなどが計算をすると自動的にint型になる。charに関しては1文字を表すときに使われるが、内部では数字として管理されており、文字として計算をすることになると自動的に数字として扱われ、外部というかソースコード上も数字として計算結果が出る。
オーバーフローとアンダーフローの違いは、その変数のデータ型に割り振られたbitの扱える組み合わせ、選択肢が何通りか決まっており。その何通りの範囲の中に数字などを当てはめていくわけだが。数字として当てはめた際に、その範囲の上限と下限を超えた際に起こる事象の事をそれぞれいう。アンダーフローは文字通り下限を超える事。オーバーフローは文字通り上限を超える事。


-----------------------------------


解答：
① result1 に最終的に入る値は/*4.0*/。理由はaとb双方がintという整数のデータ型であるから、整数で割って整数の結果しか出てこない。後からdoubleなどの浮動小数点型にしても整数から小数にならない。

② result2 に最終的に入る値は4.5。その理由はaを/*intの整数型から(double)aという明示的変換のキャストというものを行っている*/。計算をするときにdoubleの浮動小数点型にしているから計算結果も浮動小数点型の結果が出るから。

③ (double)a のような書き方をキャストと呼ぶ。元ある変数のデータ型から指定のデータ（今回はdouble）の型に変数のデータ型を明示的に変換しようとしている。

④ result1 の代入時に起きている暗黙的型変換とはc言語が自動的に元のデータ型の型から自動的に合う型変換を行う事である。

⑤ x + y の計算時に起きていることはshortという整数のデータ型からintという整数のデータ型に暗黙的変換がされている。その名称を整数の昇格という。shortより自動的にintに計算をすると自動的に変換される。

⑥ max_value の最終的な値は0。その理由として、変数に割り当てられたbitで扱える選択肢、組み合わせの数分、上限と下限の中に今回は数字を範囲として当てはめていくわけだが。選択肢、組み合わせの数分の上限を超えた範囲の数字を当てはめた結果、下限として設定されている値に変換される。この現象の名称をオーバーフローという。しかし、この巡回的な事は符号なし整数でしか起こらない。

⑦ min_value の最終的な値は255。その理由として変数に割り当てられたbitで扱える選択肢、組み合わせの数分、上限と下限の中に今回は数字を範囲として当てはめていくわけだが。選択肢、組み合わせの数分の下限を超えた範囲の数字を当てはめた結果、上限として設定されている値に変換される。この現象の名称をアンダーフローという。/*しかし、この巡回的な事は符号なし整数でしか起こらない。*/

⑧ 最後に、暗黙的型変換・明示的型変換（キャスト）・整数の昇格・オーバーフロー・アンダーフローの違いをまとめて説明してください。
暗黙型変換と明示的変換の違いはc言語が自動的に変換してくれる前者に対して、後者は人間が明示的に変換を指示、キャストという型変換に必要なものを書かないといけない。
整数の昇格はshortやcharなどが計算をすると自動的にint型になる。charに関しては1文字を表すときに使われるが、内部では数字として管理されており、文字として計算をすることになると自動的に数字として扱われ、外部というかソースコード上も数字として計算結果が出る。
オーバーフローとアンダーフローの違いは、その変数のデータ型に割り振られたbitの扱える組み合わせ、選択肢が何通りか決まっており。その何通りの範囲の中に数字などを当てはめていくわけだが。数字として当てはめた際に、その範囲の上限と下限を超えた際に起こる事象の事をそれぞれいう。アンダーフローは文字通り下限を超える事。オーバーフローは文字通り上限を超える事。