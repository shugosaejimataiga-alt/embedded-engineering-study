
問題1

次のコードについて、それぞれの変数に最終的に入る値を答えてください。

int a = 17;
int b = 5;

int result1 = a + b;
int result2 = a - b;
int result3 = a * b;
int result4 = a / b;
int result5 = a % b;

① result1
② result2
③ result3
④ result4
⑤ result5


解答：
① result1 = 22
② result2 = 12
③ result3 = 85
④ result4 = 3.4
⑤ result5 = 0

--------------

解答：
① result1 = 22
② result2 = 12
③ result3 = 85
④ result4 = /* 3 */
⑤ result5 = /* 2 */

=======================================================================


問題2

次のコードを上から順番に実行したとき、最後のxはいくつになるか答えてください。

int x = 12;

x += 8;
x -= 5;
x *= 2;
x /= 3;
x %= 4;

途中でxがどう変化するかも、自分の言葉で説明してください。


解答：
x += 8; ← これはx=12に8を足した値をxに代入する。x=20
x -= 5; ← これはx=12に5を引いた値をxに代入する。x=7
x *= 2; ← これはx=12に2をかけた値をxに代入する。x=24
x /= 3; ← これはx=12に3で割った値をxに代入する。x=4
x %= 4; ← これはx=12に4で割って出た余りをxに代入する。x=0

------------------------------------------------------

解答：
x += 8; ← これはx=12に8を足した値をxに代入する。x=20
x -= 5; ← これは/* x=20 */に5を引いた値をxに代入する。/* x=15 */
x *= 2; ← これは/* x=15 */に2をかけた値をxに代入する。/* x=30 */
x /= 3; ← これは/* x=30 */に3で割った値をxに代入する。/* x=10 */
x %= 4; ← これは/* x=10 */に4で割って出た余りをxに代入する。/* x=2 */

=======================================================================


問題3
int a = 8;
int b = 12;

int result1 = a == b;
int result2 = a != b;
int result3 = a > b;
int result4 = a < b;
int result5 = a >= 8;
int result6 = b <= 10;

それぞれ、

① result1
② result2
③ result3
④ result4
⑤ result5
⑥ result6

に入る値を、理由と一緒に答えてください。


解答：

① result1 = 0; 8と12は等しくない。間違っているから。
② result2 = 1; 8と12は等しくない数である。合っているから。
③ result3 = 0; 8は12より大きくない。間違っているから。
④ result4 = 1; 8より12の方が大きい。合っているから。
⑤ result5 = 1; 8は8以上である。合っているから。
⑥ result6 = 0; 12が10以下である。間違っているから。

=======================================================================


問題4

次のコードについて答えてください。

int a = 10;
int b = 20;

int result1 = a < 15 && b > 10;
int result2 = a > 15 && b > 10;
int result3 = a > 15 || b > 10;
int result4 = a > 15 || b < 10;
int result5 = !(a == 10);

①〜⑤に入る値と、その理由を自分の言葉で説明してください。


解答：

int result1 = a < 15 && b > 10;は1である。
&&は双方の条件があっている。なので、双方の条件に合っているから。

int result2 = a > 15 && b > 10;は0である。
&&は双方の条件があっているかなので、双方の条件に合っていないから。

int result3 = a > 15 || b > 10;は1である。
||はどちらかがあっていればよい。なので前者は違っていても後者があっているので

int result4 = a > 15 || b < 10;は0である。
||はどちらかがあっていればよい。なのですが前者も後者が違っているから。

int result5 = !(a == 10);は0である。
!は真偽を反転する。()の条件式が真=あっているので、それを反転する。


=======================================================================


問題5

次のコードを上から順番に実行したとき、xがどう変化するか答えてください。

int x = 10;

x++;
x++;
x--;
++x;
--x;

各行の後のxの値と、最後のxの値を答えてください。


解答：
x++; =11
x++; =12
x--; =11
++x; =12
--x; =11


=======================================================================


問題6

次の2つについて、xとyの最終的な値を答えてください。

// ①
int x = 10;
int y = x++;
// ②
int x = 10;
int y = ++x;

それぞれ、なぜその値になるのかも説明してください。


解答：

// ①
int x = 10;
int y = x++;
は後置インクリメントで使って+。
なのでyに10を代入した後にxに+1され11になる。
x=11,y=10

// ②
int x = 10;
int y = ++x;
前置インクリメントは先に+して使う。
xに+1された後yに代入。
x=11,y=11


=======================================================================


解答：

問題7

次の式の結果をそれぞれ答えてください。

int result1 = 5 + 2 * 3;
int result2 = (5 + 2) * 3;
int result3 = 10 > 5 && 3 < 1;
int result4 = 10 > 5 || 3 < 1;

①〜④について、どこから先に計算するかも含めて説明してください。


解答：

int result1 = 5 + 2 * 3;は*をやった後+なので11

int result2 = (5 + 2) * 3;は()をやった後に*なので21

int result3 = 10 > 5 && 3 < 1;は比較演算子<>をやった後に論理演算子なので10>5をやる。の後に3<1をやる。の後に&&をやる。1,0,をやった後&&にあっているか。なので、0である。

int result4 = 10 > 5 || 3 < 1;も10 > 5後、3 < 1後に||に合うか。1,0の後||をやって1になる。


=======================================================================



C-Day7 最終問題です。
これに合格できたらDay7終了です。

#include <stdio.h>

int main(void)
{
    int a = 10;
    int b = 3;
    int x = 5;

    int result1 = a + b * 2;
    int result2 = a / b;
    int result3 = a % b;

    x += 4;
    x--;

    int result4 = x >= 8;
    int result5 = a > 5 && b < 5;
    int result6 = a < 5 || b == 3;

    int y = x++;

    printf("%d\n", result1);
    printf("%d\n", result2);
    printf("%d\n", result3);
    printf("%d\n", x);
    printf("%d\n", y);
    printf("%d\n", result4);
    printf("%d\n", result5);
    printf("%d\n", result6);

    return 0;
}

以下を答えてください。

① result1
② result2
③ result3
④ x の最終値
⑤ y
⑥ result4
⑦ result5
⑧ result6

それぞれ、なぜその値になるのかも自分の言葉で説明してください。


解答：

① result1は*をやった後に+なので16

② result2はintなので整数に限る。10/3で/は割った後に出た値を求める。なので3

③ result3はintなので整数に限る。%は余りを求める。なので余りは1

④ x の最終値でx=5から+4された値をxに代入してデクリメントで-1するので8でy = x++でyに代入した後にxに+1する為、最終的にx=9

⑤ yはx=5から+4された値をxに代入してデクリメントで-1するので8でy = x++でyに代入した後にxに+1する為、yはそのままを代入されるため8

⑥ result4は>=は以上なので8以上であってるため1

⑦ result5はa > 5をやった後b < 5をやった後 && をやる。1,1で&&なので1

⑧ result6はa < 5をやった後b == 3をやった後 || をやる。0,1で||なので1
